<Window x:Class="wmsApp.UpgradeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:wmsApp"
        mc:Ignorable="d"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        Title="UpgradeWindow" Height="180" Width="500"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="正在下载更新..." HorizontalAlignment="Center" VerticalAlignment="Center" />

        <ui:ProgressBar Name="progressBar" Minimum="0" Maximum="100" Value="0" Height="33" Margin="20" Grid.Row="1" />

    </Grid>
</Window>
